
Install, instructions from: https://www.dropbox.com/install-linux

-------------

1) Use headless install via command line;
   cd ~ && wget -O - "https://www.dropbox.com/download?plat=lnx.x86_64" | tar xzf -

2) Run the Dropbox daemon from the newly created .dropbox-dist folder
   ~/.dropbox-dist/dropboxd

   --> This prompts you to login to your dropbox account;

   NOTE: [No longer necessary in 18.04? as of 4/2019?]
         install libpango prereq as necessary, may need to to apt install -fixup;

3) Download dropbox.py, from link on instructions page, copy to /usr/local/bin/dropbox;


4) [Maybe not? didn't seem to download anything new]
   To actually install the daemon, need to run:

     dropbox start -i
