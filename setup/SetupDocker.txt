
Now in 18.04:

apt install docker.io
apt install docker-compose

# Make /var/lib/docker point to /space/docker;
mv /var/lib/docker /space
ln -s /space/docker /var/lib/docker

# Add dpb to the docker group;
adduser dpb docker
