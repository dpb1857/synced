#!/bin/sh

echo "Run dconf-editor,"
echo "Search for 'touchpad',"
echo "reset touchpad-enabled."

echo "You can run gsettings --list-recursively to see dconf settings."
echo "(and probably even set them)"

echo "Or even look for 'touchpad' in the existing settings panel..."
